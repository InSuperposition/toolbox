# toolbox

See `CLAUDE.md` for the full design (goals, tool boundaries, GitOps flow).

The local dev OpenBao/Transit bootstrap (used by the digest-as-source-of-
truth pipeline's approval gate) is documented in
[`environments/local/README.md`](environments/local/README.md), not here —
extend this file as the reference deployment grows beyond that one piece.
