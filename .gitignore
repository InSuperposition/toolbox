.claude/
.terraform/
# tofu-rendered OpenBao server config + its raft store — regenerated by
# `mise run openbao-bootstrap`, never committed
/environments/local/openbao/
modules/*/tests/tmp/
*.tfstate*
