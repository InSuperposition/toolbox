# G1 fixture: a trivy JSON report whose only finding is LOW — proves `gate`
# filters by severity (--severity=CRITICAL), not the mere presence of
# vulnerabilities. `gate` must pass its TaskRun (step exitCode 0).
apiVersion: v1
kind: ConfigMap
metadata:
  name: gate-fixture-clean
data:
  scan.json: |
    {
      "SchemaVersion": 2,
      "CreatedAt": "2026-09-09T00:00:00Z",
      "ArtifactName": "zot.zot.svc.cluster.local:5000/cv-frontend:fixture",
      "ArtifactType": "container_image",
      "Results": [
        {
          "Target": "cv-frontend (debian 12)",
          "Class": "os-pkgs",
          "Type": "debian",
          "Vulnerabilities": [
            {
              "VulnerabilityID": "CVE-2099-00002",
              "PkgName": "libsynthetic",
              "InstalledVersion": "1.0.0",
              "FixedVersion": "1.0.1",
              "Severity": "LOW",
              "Title": "synthetic LOW finding for the T7b3 gate test",
              "Description": "Not a real vulnerability. Fixture only."
            }
          ]
        }
      ]
    }
