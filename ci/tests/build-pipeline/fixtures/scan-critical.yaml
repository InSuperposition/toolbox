# G1 fixture (ci/tests/build-pipeline/chainsaw-test.yaml): a trivy JSON
# report carrying one CRITICAL finding. Delivered as a ConfigMap so the
# chainsaw step binds it straight to the `gate` Task's `shared` workspace
# with a plain `apply` — no `kubectl create --from-file`, no shell.
#
# NOT a real scan. The single Vulnerabilities[] entry is synthetic; `gate`
# must fail its TaskRun (trivy convert --exit-code=2 -> step exitCode 2).
apiVersion: v1
kind: ConfigMap
metadata:
  name: gate-fixture-critical
data:
  scan.json: |
    {
      "SchemaVersion": 2,
      "CreatedAt": "2026-09-09T00:00:00Z",
      "ArtifactName": "zot.zot.svc.cluster.local:5000/cv-frontend:fixture",
      "ArtifactType": "container_image",
      "Results": [
        {
          "Target": "cv-frontend (debian 12)",
          "Class": "os-pkgs",
          "Type": "debian",
          "Vulnerabilities": [
            {
              "VulnerabilityID": "CVE-2099-00001",
              "PkgName": "libsynthetic",
              "InstalledVersion": "1.0.0",
              "FixedVersion": "1.0.1",
              "Severity": "CRITICAL",
              "Title": "synthetic CRITICAL finding for the T7b3 gate test",
              "Description": "Not a real vulnerability. Fixture only."
            }
          ]
        }
      ]
    }
